exports.sendEmail = async ({ to, subject, text, html }) => {
  // Placeholder for email delivery service implementation.
  console.log(`Send email to ${to}: ${subject}`);
  return Promise.resolve();
};
